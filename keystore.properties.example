# Keystore Configuration for Release Builds
# 
# IMPORTANT: 
# 1. Copy this file to 'keystore.properties' (without .example)
# 2. Replace the placeholder values with your actual keystore information
# 3. Never commit keystore.properties to version control!
# 4. Keep your keystore file and passwords safe - you'll need them for all future updates!

storePassword=YOUR_KEYSTORE_PASSWORD_HERE
keyPassword=YOUR_KEY_PASSWORD_HERE
keyAlias=mealtrack-key
storeFile=mealtrack-release-key.jks

# Instructions:
# 1. Generate keystore using:
#    keytool -genkeypair -v -storetype PKCS12 -keystore mealtrack-release-key.jks \
#      -keyalg RSA -keysize 2048 -validity 10000 -alias mealtrack-key
#
# 2. Copy this file: cp keystore.properties.example keystore.properties
# 3. Edit keystore.properties and replace placeholder values
# 4. Build release: ./gradlew bundleRelease or ./gradlew assembleRelease
